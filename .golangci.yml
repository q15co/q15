version: "2"

run:
  timeout: 5m
  go: "1.25"
  modules-download-mode: readonly

issues:
  max-issues-per-linter: 50
  max-same-issues: 10

linters:
  default: none
  enable:
    - errcheck
    - ineffassign
    - unused
  exclusions:
    presets:
      - std-error-handling
